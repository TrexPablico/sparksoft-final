import AuthClient from "@/components/auth/AuthClient";
import React from "react";

const SignIn = () => {
  return (
    <div className="mt-20">
      <AuthClient />
    </div>
  );
};

export default SignIn;
